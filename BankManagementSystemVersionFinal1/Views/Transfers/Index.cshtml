@model IEnumerable<BankManagementSystemVersionFinal1.Models.Transfer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Sender.AccountId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Receiver.AccountId)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Sender.AccountId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Receiver.AccountId)
            </td>
            
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTransfer">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTransfer">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTransfer">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

@*<a asp-controller="Managers" asp-action="Index">Back to Home</a>
*@
<a asp-action="Index">Back to Home</a>
